/**
 * The possible ways in which questions in the workflow can be displayed.  You will most likely
 * want all questions to support all display types, but if not, this can be reflected in the
 * component factory.
 */
enum class ComponentDisplayType {
    FULL_SCREEN,
    COMPACT
}